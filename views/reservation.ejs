<!-- views/reservation.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Arial', sans-serif;
      padding: 20px;
    }

    h1, h2 {
      text-align: center;
    }

    ul {
      list-style-type: none;
      padding: 0;
      text-align: center;
    }

    li {
      display: inline-block;
      margin: 5px;
      width: 30px;
      height: 30px;
      line-height: 30px;
      text-align: center;
      background-color: lightgreen;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    .booked {
      background-color: lightcoral;
    }
  </style>
  <title>Reservation Result</title>
</head>

<body>
  <div class="container">
    <h1 class="mt-4">Reservation Result</h1>
    <p>Reserved Seats: <%= reservedSeats.map((seat) => seat.seatNumber).join(', ') %> by <%= reservedBy %> at <%= reservedAt %></p>
    <a href="/" class="btn btn-primary mb-4">Back to Available Seats</a>

    <h2 class="mt-4">Updated Seat Map</h2>
    <ul>
      <% seats.forEach((seat) => { %>
        <li class="<%= seat.isAvailable ? '' : 'booked' %>">
          <%= seat.seatNumber %>
        </li>
      <% }); %>
    </ul>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@
